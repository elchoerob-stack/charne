# Build Foreman.exe.
#
# Everything Foreman is gets packed into one file: the server as a bundle, the
# console, the knowledge base and Playwright, all embedded inside a Node
# runtime. The machine it lands on needs no Node, no npm, no git and no
# terminal — the first double-click unpacks it and starts it.
#
# Run this from the Actions tab (Run workflow), or push a tag like v1.0.0 to
# get a release with the executable attached.
name: Foreman for Windows

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    # The whole build is a couple of minutes' work. Without a ceiling, a step
    # that pins a core and never returns holds a runner for GitHub's six-hour
    # maximum before anyone finds out.
    timeout-minutes: 30
    defaults:
      run:
        working-directory: cms-agent/server
        shell: pwsh

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: npm
          cache-dependency-path: cms-agent/server/package-lock.json

      - name: Install build dependencies
        run: npm ci --no-audit --no-fund

      - name: Check the code still type-checks and the tests still pass
        run: |
          # PowerShell does not stop a multi-line script when an external
          # command exits non-zero — it just runs the next line, and the step
          # is marked successful because the *last* command happened to
          # succeed. That let a real build failure slip through silently once
          # already, so every step here checks $LASTEXITCODE explicitly.
          npm run typecheck
          if ($LASTEXITCODE -ne 0) { throw "typecheck failed (exit $LASTEXITCODE)" }
          npm test
          if ($LASTEXITCODE -ne 0) { throw "tests failed (exit $LASTEXITCODE)" }

      - name: Build the payload, the launcher and the icon
        run: |
          npm run payload
          if ($LASTEXITCODE -ne 0) { throw "building the payload failed (exit $LASTEXITCODE)" }
          npm run launcher
          if ($LASTEXITCODE -ne 0) { throw "building the launcher failed (exit $LASTEXITCODE)" }
          npm run icon
          if ($LASTEXITCODE -ne 0) { throw "building the icon failed (exit $LASTEXITCODE)" }

      - name: Prepare the single-executable blob
        working-directory: cms-agent/server/build
        run: node --experimental-sea-config sea-config.json

      - name: Take a copy of the Node runtime to build on
        working-directory: cms-agent/server/build
        run: |
          Copy-Item (Get-Command node).Source -Destination Foreman.exe
          # node.exe ships signed, and neither the icon edit nor the payload
          # injection below survives that signature — so it comes off first
          # rather than being left broken. Missing signtool is not fatal: the
          # copy is then unsigned already.
          $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" -ErrorAction SilentlyContinue |
            Sort-Object FullName -Descending | Select-Object -First 1
          if ($signtool) { & $signtool.FullName remove /s Foreman.exe } else { Write-Host "signtool not found; continuing" }

      # Deliberately before the payload goes in. Run against the finished
      # 90 MB executable, rcedit pins a core and never returns — it ran for
      # six hours here once before the job timed out. Against the plain
      # runtime, with an ordinary resource table and no 5 MB blob appended,
      # it is quick.
      #
      # It is also the one step whose output nobody needs: it sets the icon
      # and the version shown in Explorer. So it gets a short leash and is
      # allowed to fail — a Foreman.exe wearing the default Node icon still
      # does every single thing it is meant to do.
      - name: Give it Foreman's icon and version info
        working-directory: cms-agent/server/build
        timeout-minutes: 5
        continue-on-error: true
        run: |
          $version = (Get-Content version.txt -Raw).Trim()
          # The rcedit npm package has no CLI of its own — `npx rcedit` fails
          # with "could not determine executable to run" because its
          # package.json declares no bin. It only ships a bundled
          # rcedit-x64.exe for its JS wrapper to spawn, so that binary is
          # installed and invoked directly instead.
          #
          # --prefix . matters: this directory (build/) has no package.json of
          # its own, and npm's default behaviour is to walk up to the nearest
          # one it finds — cms-agent/server/package.json — and install there
          # instead. Without it, rcedit lands in ../node_modules and the path
          # below silently does not exist.
          npm install --no-save --no-audit --no-fund --prefix . rcedit@5.0.2
          if ($LASTEXITCODE -ne 0) { throw "installing rcedit failed (exit $LASTEXITCODE)" }
          & .\node_modules\rcedit\bin\rcedit-x64.exe Foreman.exe `
            --set-icon foreman.ico `
            --set-version-string ProductName "Foreman" `
            --set-version-string FileDescription "Foreman - records a job once, then does it for you" `
            --set-file-version $version `
            --set-product-version $version
          if ($LASTEXITCODE -ne 0) { throw "rcedit failed to set the icon and version info (exit $LASTEXITCODE)" }

      - name: Put Foreman inside it
        working-directory: cms-agent/server/build
        timeout-minutes: 10
        run: |
          npx --yes postject@1.0.0-alpha.6 Foreman.exe NODE_SEA_BLOB sea-prep.blob `
            --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2
          if ($LASTEXITCODE -ne 0) { throw "postject failed to inject the blob (exit $LASTEXITCODE)" }

      # A short leash here too: an executable whose payload did not take runs
      # as plain node, which reads from standard input and would otherwise
      # sit there until the job times out rather than failing.
      - name: Check the executable answers for itself
        working-directory: cms-agent/server/build
        timeout-minutes: 3
        run: |
          $version = & .\Foreman.exe --version
          Write-Host "Foreman.exe reports version $version"
          if (-not $version) { throw "Foreman.exe did not report a version" }
          & .\Foreman.exe --help

      - uses: actions/upload-artifact@v4
        with:
          name: Foreman-windows-x64
          path: cms-agent/server/build/Foreman.exe
          if-no-files-found: error

      - name: Attach it to the release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          files: cms-agent/server/build/Foreman.exe
          body: |
            Download `Foreman.exe`, put it wherever you like, and double-click it.

            The first run unpacks Foreman into your account folder, generates
            an access code and fetches the browser it drives (about 150 MB,
            once). Every run after that starts in a few seconds. Nothing else
            needs installing.

            `Foreman.exe --where` prints the folders, the address for your
            phone and the access code.
